{
  "name": "davidteju.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "sass src/styling:build/styles src/projects:build/styles --watch",
    "watch:typescript": "tsc --watch",

    "start": "npm run watch:eleventy & npm run watch:sass & npm run watch:typescript",

    "build:eleventy": "eleventy",
    "build:sass": "sass src/styling:build/styles src/projects:build/styles",
    "build:typescript": "tsc",

    "build": "npm run build:eleventy & npm run build:sass & npm run build:typescript",

    "push-build": "./push-build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "sass": "^1.58.3",
    "typescript": "^4.9.5",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
